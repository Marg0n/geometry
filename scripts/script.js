function calculateArea() {
  //base and lenght
}

function getInputById(element) {
  const inputFieldText = document.getElementById(element).value;
  const innputValue = parseFloat(inputFieldText);
  return innputValue;
}
